<%#
# New

This view is the template for the "new resource" page.
It displays a header, and then renders the `_form` partial
to do the heavy lifting.

## Local variables:

- `page`:
  An instance of [Administrate::Page::Form][1].
  Contains helper methods to help display a form,
  and knows which attributes should be displayed in the resource's form.

[1]: http://www.rubydoc.info/gems/administrate/Administrate/Page/Form
%>



<% content_for(:title) { "Create New #{page.resource_name.titleize}" } %>


<section class="container">
  <div class="row">
    <header class="header">
      <h1 class="text-center"><%= content_for(:title) %></h1>
      <div class="text-center">
        <%= link_to 'Lawyers Dashboard', :back, :class => 'btn btn-success btn-sm' %>
        <%= link_to 'Category Dashboard', admin_categories_path, :class => 'btn btn-success btn-sm' %>
        <%#= link_to 'Dashboard', :back, :class => 'btn btn-success btn-sm' %>
      </div>
    </header>
  </div>
</section>

<%= render 'shared/three_breaks' %>

<%= render 'form', page: page %>
